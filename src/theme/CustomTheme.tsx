import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Arial, sans-serif', // Thay 'Arial, sans-serif' bằng font family mà bạn muốn sử dụng
  },
  
});

export default theme;